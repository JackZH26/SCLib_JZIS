"""SCLib_JZIS ingestion pipeline.

Public entry point is `ingestion.pipeline.main` (also exposed as the
`sclib-ingest` console script). Everything else is internal and may change.
"""
__version__ = "0.1.0"
