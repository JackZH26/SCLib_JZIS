# RG04 engineering delivery — 2026-09-21

Status: implemented and verified locally. The complete ordinary API regression
at `5d1bca3cc0a216f0a752dee7efd2667029804cd9` passed 7,460 cases with three
explicit opt-in skips, each separately rerun and passed using its original local
reference files. All 224 modules and 16 owned-service batches completed.
GitHub workflow authorization still blocks remote delivery. No scientific issue
is closed and no production operation has occurred.

## Implemented result

Schema 0078 records append-only result–original-passage review decisions with
exact extraction, evidence, source, claim/sample and predecessor hashes. Private
context, preview, commit, withdrawal and actor-scoped receipt routes require
current reviewer authority. Preview runs the real SQL insertion then rolls back;
commit/replay and competing writers cannot silently fork a pair's history.

The Ask consumer resolves links in the same transaction as the final source
check. Missing, withdrawn, stale and unauthorized links produce no positive
relation. Version 1.1 displays qualified reviewed relations; version 1.0 saved
answers retain their original closed fields. Neither version invents numerical
or causal explanation, scientific acceptance or canonical physical specimen
identity from an extraction-record hash.

A reviewer can durably withdraw an intact historical pair after the source
changes. Changed immutable hashes still fail, and both service and SQL guards
refuse stale establishment. Restoring the source does not resurrect a withdrawn
link. The operator sequence is documented in
[Mixed scientific retrieval](../../MIXED_SCIENTIFIC_RETRIEVAL.md).

Migration verification covers empty-ledger round trips, real review/replay and
nonempty downgrade refusal. The actual two-instance recovery retains current
and withdrawn pairs, original receipts, all public-table bytes, artifact bytes
and index reconstruction. The fixture verifier uses its exact paper/reviewer;
another capsule's lawful history is preserved and cannot cause a global-count
false failure. Missing papers and wrong reviewers still fail.

Seven native SQL/HTTP captures were regenerated as `delivery20260921r3` after
the recovery repair. The actual 1.1 mixed-response archive remains at its
`delivery20260921r2` identity. All older captures remain unchanged. Frontend
source-pin checks compare the new captures with actual current backend bytes.

## Verification

These are native macOS observations using owned disposable services and
synthetic fixtures, except for the separately identified historical reference
bytes. Diagnostic root: `/private/tmp/sclib-rg04-20260921-hs6iuj54`.
The [latest receipt index](delivery-2026-09-21/restore-scope-revision/receipt-index.json)
records file hashes. Evidence paths below are relative to
`delivery-2026-09-21/`. Test scopes overlap; these rows are not a combined count.

| Check | Result | Retained evidence |
| --- | --- | --- |
| RG04, mixed/history contracts, currentness and earlier captures | 183 passed | stale-withdrawal-revision/focused-api-r3.xml |
| Repaired restore worker and latest seven captures | 21 passed | restore-scope-revision/restore-captures-r3.xml |
| Complete offline scripts | 2,305 passed plus 91 subtests | restore-scope-revision/offline-full-r3.xml |
| Migration | 0078; 26 outcomes; 741 unchanged inputs; owned cleanup | restore-scope-revision/schema-rehearsal-r3.json |
| Actual release recovery | dump/copy/restore/verify/index rebuild/source recheck; owned cleanup | restore-scope-revision/restore-r3.json |
| Frontend | 1,890 component and 46 source tests passed; TypeScript passed | restore-scope-revision/frontend-r5.log |
| Research browser | 42 passed; zero failed/skipped/flaky | restore-scope-revision/research-browser-r4.json |
| Complete ordinary API | 7,460 passed; 3 explicit opt-in skips; all 224 modules in 16 batches; 741 unchanged inputs and 16 owned cleanups; 5,100.105 s | restore-scope-revision/api-regression/plan.json, result.json, batch-01–16.xml and log |
| Explicit-path reference canaries | 3 passed; original byte pins and no-write replay; owned cleanup | restore-scope-revision/reference-canaries-final.xml and .log |
| Capacity boundary | 1 passed; owned cleanup | stale-withdrawal-revision/capacity-final.xml |
| Security scan | 561-commit full scan plus the two subsequent source commits and staged delivery scan: zero remaining findings; exact-fingerprint regression 14 passed | stale-withdrawal-revision/gitleaks-history-r2.log; restore-scope-revision/gitleaks-incremental-r3.log, gitleaks-delivery-final.log and security-fingerprints-r3.xml |
| Public production build/browser | 13 passed; production frontend code unchanged since that check | public-browser.json |
| Linux Test/Security and final image parity | Not run; remote authorization required | No native substitute |

The ordinary invocation leaves three opt-in reference cases skipped because no
local reference paths are supplied. A separate invocation supplied the already
retained QEF/q-e capsule and force-constant paths: all three passed in 5.35 s.
It preserves one pending import and two quarantines, with zero scientific or ML
admissions. No reference bytes were downloaded, changed or executed as a new
scientific calculation. These are the same three cases, not additional ordinary
suite coverage.

Implementation: `20f8542`; stale-withdrawal repair: `83096af`; recovery-scope and
browser-test repair: `c16f164`; final scanner policy: `5d1bca3`. API and scripts
remained unchanged throughout the final complete API run; the final coordinator
exited 0 and independently recounted JUnit outcomes match its summary. Latest
migration and recovery receipts retain their actual `bff62fd` dirty-worktree identities.
Their inventories match 740 of 741 files at `5d1bca3`; the sole later change
pins two reviewed scanner fingerprints in its separately passing regression.
No receipt is resealed or relabeled as a later revision.

## Defects found and retained evidence

The first stale-withdrawal regression reproduced refusal after source change;
its repaired 12-case module passed. The initial full API run was interrupted
for that repair and is not complete-suite evidence. Its two completed batches,
plan and incomplete result remain under `interrupted-api`. The next full run at
`bff62fd` stopped at batch 4 with 2,236 passes and one recovery-scope failure;
its plan, result and all four completed-batch JUnit files remain under
`stale-withdrawal-revision`.
The repaired worker passes all 14 cases, including two independent capsules,
unchanged unrelated history, missing-source and wrong-reviewer rejection.

One browser run had 41 passes and one mobile assertion failure: it assumed all
six Strict Mode fetches reach the interceptor, although an aborted fetch can
stop earlier. The test now checks the bounded reads for each completed mount
and still requires exactly one new GET for explicit retry, with no diagnostic
leak or unexpected/external calls. The full rerun passed 42 cases without
raising timeouts or using retries; the newest capture rerun also passes all 42
cases, as listed above.
An earlier component focus assertion now waits for React's actual focus effect,
retaining the same focus assertion and timeout.

## Remaining acceptance

See the [ordered remaining acceptance](Remaining_Acceptance_2026-09-21.md) and
[prepared PR description](Implementation_PR_Draft_2026-09-21.md). Remote PR/CI,
ML08's real source selection and human reviews (both still zero), real RG04
pairs and gold evaluation, ML09's independent pilot/execution admission,
empirical RPS/ML evaluation, reviewed Discovery rows and production cutover
acceptance remain open. The existing production ingestion pause is preserved.
