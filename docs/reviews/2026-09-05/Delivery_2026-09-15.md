# Priority delivery — 2026-09-15

Status: local engineering verification completed; remote delivery is blocked by the
GitHub credential's missing workflow scope. No issue is closed by this record.
This supersedes the September 13 draft's publishing-authorization and running
batch83 claims. The user requested completion of outstanding priorities on
September 15.

## Delivered engineering changes

- Preserve the accumulated upgrade branch history and batches 74–83.
- Refresh Next.js to 15.5.24, PostCSS to 8.5.23, cryptography to 50.0.1 in both
  Python locks, and affected frontend transitive dependencies. Fresh locked
  dependency audits report zero known vulnerabilities. The application retains
  Plotly basic/gl2d imports; no MapLibre compatibility claim is made.
- Carry forward the deployed checkout's September 3 ingestion pause guard and
  maintenance backup units. Paused entrypoints exit before ingestion;
  maintenance backups can explicitly skip pruning. Existing host-local pause
  state is preserved. No production marker, scheduler or live data was changed.
- Repair release-restore source validation. The existing collector included
  api/services/ml08_pilot.schema.json, but the restore validator refused it.
  The fix accepts the migration reporter's narrow packaged-schema naming rule,
  preserves exact hash/currentness checks, and refuses arbitrary JSON, hidden,
  nested and traversal paths. An actual repository-inventory test reproduced
  the defect before the fix.
- Refresh seven native SQL/HTTP interface captures into separately identified
  delivery20260915r2 archives and update all current frontend consumers.
  Historical archives remain unchanged. Scanner exceptions are exact reviewed
  immutable fingerprints for synthetic request identifiers, with an exact-set
  regression; there are no broad secret-scanner exclusions.
- Repair the isolated public-browser production build's standalone tracing.
  Copied source and linked dependencies remain inside the tracing root while
  production configuration and the original build output are preserved.
- Exclude frontend/tmp from the application TypeScript project. Actual config
  expansion found 149 generated-copy files entering the original compiler.
  The new regression retains the real app layout and excludes owned browser
  copies; the production build and browsers pass after the correction.
- Document durable log capture, the permission-test-compatible umask and exact
  sanitized-environment tokenizer preparation without weakening test guards.

## Verification and scope

Private diagnostic root: /private/tmp/sclib-delivery-20260915-cvlj6qk5.
These are native macOS / Python 3.12.14 / Node 25.5.0 results. Remote CI uses
Linux / Python 3.11 / Node 20 and has not run for this delivery. Owned-service
checks use disposable PostgreSQL/Redis. Browser transports use synthetic
fixtures and reject unexpected external requests. Passing these checks does
not constitute scientific or production acceptance.

| Check | Latest result | Receipt in diagnostic root |
| --- | --- | --- |
| Locked API and ingestion dependency audits | 0 known findings each | api-audit-fixed.json; ingestion-audit-fixed.json |
| Frontend audit, including development dependencies | 0 known findings | frontend-audit-fixed.json |
| Ordinary API regression | 7,440 passed, 3 explicit reference opt-in skips; all 223 modules in 8 batches, zero failures/errors, all runner exits zero | api-regression-final/result.json; plan.json; eight JUnit receipts |
| Ingestion suite | 1,275 passed | ingestion-tests.xml |
| Full offline scripts after restore fix | 2,299 passed plus 91 subtests | offline-scripts-restored.xml |
| Restore contract/runner/index/worker regressions | 308 passed | restore-provenance-verified.xml |
| Native interface captures after restore fix | 7 passed | native-captures-r2.xml; capture-manifest-r2.json |
| Frontend components | 1,887 passed, with two workers and unchanged assertions/timeouts | frontend-unit-r2.log |
| Frontend source checks after TypeScript fix | 46 passed | frontend-source-final.log; frontend-source-integrated.log |
| Research browsers | 42 passed | research-browser-r2/results.json |
| Production public build and browsers after TypeScript fix | 13 passed | public-browser-final.json |
| Full Git history scan | 555 commits, zero remaining findings; subsequent TypeScript-only commit also clean | history-scan-restored.json; typescript-fix-scan.json |
| Exact scanner-policy regression | 14 passed | Recorded terminal result at d5880ab |
| Installed API wheel replay | 3 cases and corrupt-context rejection passed; accounting/registration/review probes also passed | installed-evidence-final.json |
| Native migration rehearsal | Head 0077; 23 outcomes, retention/read-model rollback, 735 unchanged inputs and owned cleanup verified | schema-rehearsal-restored.json |
| Native release recovery | Actual dump/copy/restore/verify/index rebuild/source recheck, 735 unchanged inputs and owned cleanup verified | research-restore-delivery.json |
| Separate review-payload capacity boundary | 1 passed | capacity-final.xml |
| Existing genuine QE reference canaries | 3 passed; exact no-write replay and owned cleanup | reference-final.xml |
| Remote Linux Test/Security and exact release image parity | Not run; remote delivery blocked | No local result substitutes for these checks |

Selected verbatim receipts and full-file SHA-256 values are retained in the
[delivery receipt index](delivery-2026-09-15/receipt-index.json). The QE checks
reuse previously documented read-only reference files. They involve no new
calculation, download, source redistribution or scientific acceptance. Their
three passes and the separate capacity pass are not added to ordinary API counts.

## Revision identity

The complete API coordinator finished against 6c1e15abbd5ea1d714577b9db4a99408d4a23889.
Its HEAD and API/script source inventory remained unchanged throughout the run.
All eight batches completed, all 223 ordinary modules were covered, and every
owned runner exited zero after cleanup. The three opt-in reference skips were
separately executed against existing genuine reference files, as recorded above.

The independently verified integration branch is
codex/sclib-restore-provenance-20260915 at
5d4aad086551b23e77abf4328aea3684fb89f842. It contains restore repair fb2d575,
r2 captures 2374b5a, exact scanner policy d5880ab and TypeScript fix 5d4aad0.
It was fast-forward integrated into codex/sclib-research-v2 after the main API
coordinator completed. The API subtree
and guarded disposable runner are unchanged from 6c1e15a; the restore-only
implementation delta has the separate complete offline and actual recovery
verification recorded above.

The final migration and restore receipts retain their actual d5880ab identity.
Both sets of 735 pinned API/script inputs match 5d4aad0 exactly; its subsequent
TypeScript/documentation changes do not alter those inputs. No receipt has been
relabeled to pretend it tested a later revision.

The integrated main worktree also passed all 46 frontend source checks, including
actual TypeScript input expansion with its retained browser workspace present.
The archived receipt directory passed a separate sensitive-information scan.

## Earlier failed or incomplete attempts

Passing API batches retain non-failing FastAPI regex and Alembic path_separator
deprecation warnings in their original logs. These warnings were not suppressed.

The September 14 batch83 coordinator terminated with BrokenPipeError, exit 120,
and is incomplete. Initial September 15 API/migration attempts were stopped
before refreshing dependency locks. The first scripts invocation imposed umask
077 and lacked a tokenizer cache in its sanitized environment: one permission
failure and 83 setup errors were corrected through invocation setup, retaining
all security assertions. The first refreshed component run had two timeout
failures under heavy concurrent load; the final complete run bounded workers
without selecting fewer tests or relaxing assertions/timeouts. The initial
public build tracing failure and later TypeScript input leak were reproduced
and corrected. The first restore invocation refused the packaged schema path;
the red regression and successful full native restoration are retained.

## Remote delivery and remaining work

Authenticated account JackZH26 can access JackZH26/SCLib_JZIS, but its OAuth
credential has repo/read:org/gist and lacks workflow scope. GitHub expressly
rejected the workflow-file push. Default and configured personal SSH identities
also failed access. No workflow was removed and no credential restriction was
bypassed. The required user action has already been requested:
`gh auth refresh -h github.com -s workflow`. There is no published upgrade
branch or submitted PR from this delivery yet. The prepared English draft is
[Implementation_PR_Draft_2026-09-15.md](Implementation_PR_Draft_2026-09-15.md).
No main merge, deployment, migration, ingestion activation or issue closure
occurred.

The [ordered remaining acceptance audit](Remaining_Acceptance_2026-09-15.md)
separates engineering delivery from actual missing implementation: reviewed
positive result/passage associations and authorized real-dataset ML execution.
Independent human pilot/source-rights decisions, empirical ML/RPS/RAG evidence,
real reviewed Discovery rows and production release acceptance also remain.
Human pilot completion is not imposed as a new prerequisite for closing bounded
engineering defects when their own issue criteria are satisfied.
