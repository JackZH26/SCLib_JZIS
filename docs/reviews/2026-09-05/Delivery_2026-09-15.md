# Priority delivery — 2026-09-15

Status: engineering verification and draft delivery in progress. This record
supersedes the September 13 draft's pending publishing authorization and active
batch83 run claim. The user requested completion of outstanding priorities on
September 15. No issue is closed by this record.

## Changes

- Preserve accumulated batches 74–83 and the existing upgrade branch history.
- Update Next.js to 15.5.24, PostCSS to 8.5.23, cryptography to 50.0.1 in both
  Python locks, and vulnerable frontend transitive dependencies. Fresh pnpm and
  pip-audit reports contain zero known vulnerabilities. This does not replace
  container scanning or guarantee absence of undisclosed vulnerabilities.
  Plotly uses its existing basic/gl2d bundles; the overridden MapLibre package
  is not imported by the application and no map compatibility is claimed.
- Carry forward the September 3 operational ingestion pause guard and maintenance
  backup units from the deployed checkout. Preserve host-local pause state;
  paused entrypoints exit before ingestion, while maintenance backup can skip
  pruning. No scheduler, production marker or live data was changed.
- Capture seven current native SQL/HTTP contracts into new delivery20260915
  archives and update frontend references. Historical archives remain unchanged.
- Repair public-browser standalone file tracing: keep both copied source and
  linked dependencies inside the tracing root, retain production configuration,
  and keep the developer's build output untouched.
- Document durable test logs and exact-environment tokenizer preparation.

## Terminal evidence

Private diagnostic root: `/private/tmp/sclib-delivery-20260915-cvlj6qk5`.
These are native macOS / Python 3.12.14 / Node 25.5.0 results, not Linux image
parity. All API services are owned disposable instances. Browser transports use
synthetic data and forbid unexpected external browser requests.

| Check | Result | Receipt |
| --- | --- | --- |
| Locked API and ingestion dependency audits | 0 findings each | api-audit-fixed.json; ingestion-audit-fixed.json |
| Frontend dependency audit including development dependencies | 0 findings | frontend-audit-fixed.json |
| Ingestion suite | 1,275 passed | ingestion-tests.xml |
| Seven native interface captures | 7 passed, 112.08 seconds | native-captures.xml; capture-manifest.json |
| Research browser suite | 42 passed, 3.4 minutes | research-browser/results.json |
| Pause/backup/credentials targeted checks | 15 passed | local terminal receipt |
| Full API, migration, offline scripts, frontend/public browser and remote CI | Pending final verification | Do not infer completion from earlier partial runs |

The September 14 batch83 API coordinator terminated with BrokenPipeError and
exit 120; it is incomplete. Early September 15 API/migration attempts were
intentionally stopped before refreshing dependency locks. The first current
scripts attempt had 2,208 passes, one permission-mode failure and 83 tokenizer
setup errors: the invocation imposed umask 077 and had not warmed the tokenizer
under its sanitized temporary-directory environment. Correct the invocation,
not the permission guard. The first refreshed frontend run had 1,885 passes and
two timeout failures during concurrent heavy checks; rerun without relaxing
assertions or default timeouts. The first public build failed in standalone
file tracing; the launcher correction requires a full build/browser rerun.

## Remaining acceptance boundaries

Finish exact-revision engineering checks and publish a draft PR with actual
receipts. Keep the 37 execution issues open until their individual criteria are
mapped to delivered evidence. Genuine independent human pilot review, source
rights decisions, empirical ML/RPS and RAG evaluation, production restore and
release acceptance remain outstanding. Synthetic fixtures, passing unit tests
and reconstructed evidence do not satisfy these scientific or operational gates.
