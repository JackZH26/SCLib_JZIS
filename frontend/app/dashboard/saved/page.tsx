import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function SavedPage() {
  return (
    <ComingSoon
      title="Bookmarks"
      blurb="Save papers and materials for later with a ★ button on detail pages — coming in the next release. The API already supports creating and listing bookmarks today."
    />
  );
}
