import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function FeedbackPage() {
  return (
    <ComingSoon
      title="Feedback"
      blurb="Send bug reports, feature requests, or data issues straight to the JZIS team. The POST /feedback endpoint is live; the dashboard form lands in the next release."
    />
  );
}
